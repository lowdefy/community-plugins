{
  "name": "@lowdefy/community-plugins",
  "license": "MIT",
  "packageManager": "pnpm@8.8.0",
  "private": true,
  "scripts": {
    "changeset": "changeset",
    "publish": "changeset publish",
    "test": "pnpm -r test",
    "update-packages": "pnpm up -i -r --latest",
    "release:version": "pnpm changeset version && pnpm install --no-frozen-lockfile",
    "release:publish": "pnpm -r publish",
    "release:version-experimental": "pnpm changeset version --snapshot experimental && pnpm install --no-frozen-lockfile && pnpm -r --if-present build",
    "release:publish-experimental": "pnpm -r publish --tag experimental --no-git-checks"
  },
  "devDependencies": {
    "@changesets/cli": "2.26.2",
    "eslint": "8.53.0",
    "eslint-config-prettier": "9.0.0",
    "eslint-import-resolver-node": "0.3.9",
    "eslint-plugin-import": "2.29.0",
    "eslint-plugin-jsx-a11y": "6.8.0",
    "eslint-plugin-prettier": "5.0.1",
    "eslint-plugin-react": "7.33.2",
    "eslint-plugin-react-hooks": "4.6.0",
    "prettier": "^3.0.3"
  }
}
